$kind: collection
name: SpendWise
variables:
  baseUrl: /
  bearerToken: ""
auth:
  - id: 3e961a0f-8aa6-44b6-8977-f76b6bc3cd60
    type: bearer
    name: bearer auth
    credentials:
      token: "{{bearerToken}}"
