$kind: http-request
name: /api/Authentication/login
url: "{{baseUrl}}/api/Authentication/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "password": "hasan12345",
        "userName": "hasan_awata"
    }
examples: ./.resources/-api-Authentication-login.resources/examples
order: 1000
