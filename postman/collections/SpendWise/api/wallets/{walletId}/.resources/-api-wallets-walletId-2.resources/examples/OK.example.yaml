$kind: http-example
request:
  url: "{{baseUrl}}/api/wallets/:walletId"
  method: DELETE
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    walletId: "1624"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
