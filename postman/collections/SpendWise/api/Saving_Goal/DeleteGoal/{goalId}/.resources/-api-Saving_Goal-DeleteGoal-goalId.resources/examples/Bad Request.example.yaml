$kind: http-example
request:
  url: "{{baseUrl}}/api/Saving_Goal/DeleteGoal/:goalId"
  method: DELETE
  headers:
    - key: Accept
      value: text/plain
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    goalId: "348"
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "type": "string",
        "title": "string",
        "status": 3829,
        "detail": "string",
        "instance": "string"
      }
order: 2000
