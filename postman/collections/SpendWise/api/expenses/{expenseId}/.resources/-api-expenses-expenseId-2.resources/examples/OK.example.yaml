$kind: http-example
request:
  url: "{{baseUrl}}/api/expenses/:expenseId"
  method: DELETE
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    expenseId: "5843"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
