$kind: http-example
request:
  url: "{{baseUrl}}/api/expenses/:expenseId"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    expenseId: "348"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
