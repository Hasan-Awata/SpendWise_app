$kind: http-example
request:
  url: "{{baseUrl}}/api/tags/:tagId"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    tagId: "9299"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
